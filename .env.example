# This is an example .env file for a web application using Docker Compose.
# You can copy this file to .env and set your own values.

# Backend config
BACKEND_PORT=8080

# Frontend config
FRONTEND_PORT=3000

# Database config
DB_HOST=localhost
DB_PORT=5432
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=mydb

# MinIO config
MINIO_ENDPOINT=http://minio:9000
MINIO_CONSOLE=http://localhost:9001
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
